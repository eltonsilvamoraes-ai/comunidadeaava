/* =====================================================================
 * CONFIGURAÇÃO DO SUPABASE
 * Pegue estes dois valores em: Supabase > Settings > API
 *   - URL      = "Project URL"
 *   - ANON_KEY = "Project API keys" > "anon public"
 * (A anon key pode ficar no frontend — é pública. Quem protege os dados
 *  é a RLS, não o segredo da chave.)
 * ===================================================================== */
window.SUPA_CONFIG = {
  URL:      'COLE_AQUI_O_PROJECT_URL',
  ANON_KEY: 'COLE_AQUI_A_ANON_PUBLIC_KEY'
};
